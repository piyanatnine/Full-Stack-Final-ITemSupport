{
  "$jsonSchema": {
    "required": [
      "itemCode",
      "name",
      "owned",
      "inStock"
    ],
    "properties": {
      "itemCode": {
        "bsonType": "string",
        "description": "Item code, string (required)"
      },
      "name": {
        "bsonType": "string",
        "description": "Item name, string (required)"
      },
      "description": {
        "bsonType": "string",
        "description": "Item description, string"
      },
      "imageUrl": {
        "bsonType": "string",
        "description": "Image url, string"
      },
      "owned": {
        "bsonType": "int",
        "description": "Number of item owned, int (required)"
      },
      "inStock": {
        "bsonType": "int",
        "description": "Number of item in stock, int (required)"
      }
    }
  }
}